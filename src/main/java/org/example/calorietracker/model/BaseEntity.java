package org.example.calorietracker.model;

/**
 * Базовый интерфейс-маркер для сущностей предметной области.
 *
 * <p>Основное назначение:
 * <ul>
 *   <li>Определение общего типа для всех JPA-сущностей приложения</li>
 *   <li>Использование в generic-методах (например, в {@code ReferenceMapper})</li>
 *   <li>Обеспечение типобезопасности при работе с репозиториями и мапперами</li>
 * </ul>
 *
 * <p>Все сущности предметной области должны реализовывать этот интерфейс.
 * Реализующие классы должны соответствовать требованиям JPA.
 */
public interface BaseEntity {
    /**
     * Предполагается, что реализующие классы будут содержать:
     * <ul>
     *   <li>Поле {@code id} с аннотацией {@code @Id}</li>
     *   <li>Реализацию {@code equals()} и {@code hashCode()} на основе id</li>
     *   <li>JPA-аннотации для маппинга на таблицы БД</li>
     * </ul>
     */
}
